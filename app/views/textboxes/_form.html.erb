<%= form_with(model: textbox, local: true, id: 77) do |form| %>
  <div class="field">
    <%= form.text_area :content, id: :textbox_content %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
